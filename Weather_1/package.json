{
  "name": "weather-widget",
  "version": "1.0.0",
  "description": "A widget that detects the user’s location and displays real-time weather data.",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "typescript": "^4.1.3",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4"
  }
}