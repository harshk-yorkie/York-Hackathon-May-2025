export class WeatherAPI {
  getWeatherData(city: string) {
    console.log(`Fetching weather data for ${city}...`);
    // TODO: Implement the functionality to fetch weather data from OpenWeatherMap API
  }
}