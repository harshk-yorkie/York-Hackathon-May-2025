import axios from 'axios';

class WeatherService {
  static async getWeatherData(cities: string[]) {
    // Call OpenWeatherMap API for each city and return the results
  }
}

export default WeatherService;